function count_Function() {
    document.ElementById("counting").innerHTML = Count();
	function Count() {
	    var Starting_point = 9;
		function plus_one() {starting_poimt += 1;}
		plus_one();
		return starting_point;
	}
}