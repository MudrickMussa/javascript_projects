function more_Math() {
    var simple_Math = (1+2) * 10 / 2 -5;    // function using than one operators.
	document.getElementById("Math").innerHTML = "1 plus 2, multiplied by 10,
	divided in half and then sustracted by 5 eguals "+ simple_Math;
	
	}
	
	window.alert(Math.random() *100); // function using Math.random()
	
	
	